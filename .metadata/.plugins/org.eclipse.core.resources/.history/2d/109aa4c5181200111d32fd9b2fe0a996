package com.example.demo.feign;
@FeignClient(name = "RISK-SERVICE")
public interface RiskClient {

    @PostMapping("/api/risk/{customerId}/{year}")
    RiskResponseDTO assess(
            @PathVariable Long customerId,
            @PathVariable Integer year);
}